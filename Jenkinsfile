pipeline {
    agent any

        stage("Shell Script"){
            stages{
            steps {
//             parallel(
                sh 'mkdir testing'
                sh 'mkdir testing2'
                sh 'ls' 
//                 sh 'chmod -R a+wx testing'
//                 )
                }
            }
        }
}
        
        
 
